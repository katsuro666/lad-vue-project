export enum RouteNames {
  HOME = 'Home',
  AUTH = 'Auth',
  ABOUT = 'About',
  LOGIN = 'Log in',
  SIGNUP = 'Sign up'
}

export enum RoutePaths {
  HOME = '/',
  AUTH = '/auth',
  ABOUT = '/about',
  LOGIN = '/login',
  SIGNUP = '/signup',
  NOTFOUND = '/:catchAll(.*)*'
}
