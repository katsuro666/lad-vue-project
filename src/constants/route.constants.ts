export enum RouteNames {
  HOME = 'Home',
  AUTH = 'Auth',
  LOGIN = 'Log in',
  SIGNUP = 'Sign up'
}

export enum RoutePaths {
  HOME = '/',
  AUTH = '/auth',
  LOGIN = '/login',
  SIGNUP = '/signup',
  NOTFOUND = '/:catchAll(.*)*'
}
